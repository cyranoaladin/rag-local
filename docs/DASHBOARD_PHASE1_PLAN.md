# Dashboard Phase 1 Plan
- Backend FastAPI (auth, folders, documents, ingestion logs)
- Postgres service (compose) + Alembic
- UI: Streamlit first (JWT cookie), pages: overview/ingestion/folders/history
- Nginx: new vhost path /dashboard (internal reverse proxy)
