services:
  ollama:
    deploy:
      resources:
        limits:
          cpus: "3.5"  # GitHub runners expose 4 vCPUs; keep some headroom to avoid compose errors
          memory: 12G
  ingestor:
    ports:
      - "127.0.0.1:18001:8001"
  ui:
    ports:
      - "127.0.0.1:18501:8501"
