# ═══════════════════════════════════════════════
# RAG Service v2 — Python Dependencies
# ═══════════════════════════════════════════════

# FastAPI + serveur
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.8.0
pydantic-settings==2.5.0

# pgvector (remplace chromadb-client)
asyncpg==0.29.0
psycopg[binary,pool]==3.2.1
pgvector==0.3.2

# Embeddings HTTP client
httpx==0.27.2

# Redis + Celery (async ingestion + cache)
redis[hiredis]==5.0.8
celery==5.4.0

# Reranking
sentence-transformers==3.0.1

# Document parsing
pypdf==4.2.0
python-docx==1.1.2
beautifulsoup4==4.12.3
unstructured==0.15.0

# Chunking
langchain==0.2.14
langchain-community==0.2.12
langchain-text-splitters==0.2.4
langchain-google-community[drive]==1.0.7

# Ollama client (compatibilité v1)
ollama==0.3.0

# Prometheus metrics
prometheus-client==0.21.0

# HTTP requests (compatibilité v1 + admin_api)
requests==2.32.3

# File upload support
python-multipart>=0.0.9

# Testing
pytest==8.3.0
pytest-asyncio==0.24.0
pytest-cov==5.0.0
